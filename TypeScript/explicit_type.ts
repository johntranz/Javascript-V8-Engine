// EXPLICIT TYPES
let myName2: string;
let age: number;
let isAuthenticated: boolean;

// arrays
let students: string[] = ['nam', 'huy'];

// union
let union: string | number | boolean;

// enum
let hobby: 'book' | 'music';
hobby = 'book';

// objects
let people: object;
people = { name: 'hihi', age: 30 };
people = [];

let student: {
  name: string;
  age: number;
  isGood: boolean;
};

student = {
  name: 'huy',
  age: 50,
  isGood: true,
};
